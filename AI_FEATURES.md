# AI 功能与验证说明

支持多 API 配置、精确模型路由、暂停与顺序对话、当前页及整份 PDF 视觉读取。

## 配置与切换

1. 打开「设置 → AI」，点击顶部「添加 API」填写任意兼容接口；也可从「从模板添加」选择 DeepSeek 或 OpenAI。当前支持 OpenAI 兼容的 Chat Completions 接口。
2. 为每个接口填写唯一名称、Base URL 和密钥，例如「DeepSeek 个人」与「DeepSeek 工作」。Base URL 不带 `/chat/completions`。每个接口的密钥分别存入钥匙串。
3. 添加服务商提供的精确模型 ID；支持图片输入的型号勾选视觉能力。上下文预算按模型实际能力设置。
4. 保存后，在窗口底部模型选择器中选择「API 名称 · 模型 ID」。

DeepSeek 模板包含 `deepseek-v4-flash`、`deepseek-v4-pro`、`deepseek-v4-flash-vision-exp`。视觉型号的图像输入格式依据 [DeepSeek 官方视觉文档](https://api-docs.deepseek.com/guides/vision/)；实际可用型号仍取决于所配置服务商和账户。

一个选择项绑定 API 地址、独立密钥账户和精确模型 ID。请求开始时固定这一组合，中途切换选择器只影响下一次新问题。删除或失效的选择不会自动回退到其他模型。

AI 设置采用单栏布局，顶部下拉菜单切换待编辑的 API，没有 API 侧栏。窗口支持拉高、拉宽，较长内容使用单层滚动区域，保存按钮固定在底部；模型的上下文预算可展开调整。

回答显示请求的 API/模型和服务端返回的模型 ID。返回不同型号时停止并报错，不自动改用 Pro 等其他模型；同名附日期的模型快照允许通过。服务端没有返回型号时明确显示无法核验。客户端无法独立证实服务商内部实际运行的模型，只能保证请求字段并校验其响应声明。

## 暂停与顺序回答

- 回答按句末标点或换行逐段显示；结束或暂停时显示已收到的剩余半句话。
- 点击「暂停」取消当前任务及网络读取，并保存已收到内容；等待旧任务退出后才允许新请求。
- 点击「继续回答」使用原 API 和模型发起新请求续答。它不是恢复原网络连接；原路由已删除或改变时明确报错。
- 所有窗口共用一个生成名额，普通聊天、上下文摘要、PDF 分批读取和全文总结依次执行。每次请求固定 `n: 1`。

## 视觉读取当前页或整份 PDF

打开 PDF、选择支持图片输入的模型后，点击「读取当前页」，只发送点击时正在看的那一页。可以先输入重点问题；留空则解释当页的文字、公式和图表。消息与请求保留原文档页码，不额外请求全文总结。翻页不会改变已开始的任务；暂停后续读仍使用原页和原 API，文档编辑或关闭则停止读取。

打开 PDF、选择支持图片输入的模型后，点击对话区「视觉读取整份 PDF」。可以先在输入框写下重点问题；留空则使用默认阅读任务。点击后会把页面图像发送到当前选择的 API。

程序将全部页面按两页一批渲染为 JPEG，等待当前批次回答完成后再读取下一批，显示完成页数并保存逐页笔记。所有页面完成后，再按上下文预算顺序整理笔记、生成全文总结。全文总结单独显示，较长笔记可展开查看。

每批只保留所需页面图像，最长边限制为 1536 像素。扫描页、图表和页面布局会进入视觉输入，但很小的文字可能无法辨认；全文总结也可能遗漏细节。

暂停保留已完成批次；同一文档未改变时，续答从未完成的批次重新读取。读取期间关闭或修改文档会停止并明确提示，防止混用不同版本页面。重启应用后需要重新点击读取按钮建立文档读取任务。

## 开发验证

本地回归使用模拟 API 和测试钥匙串，不读取真实密钥、不向 AI 服务上传文档。运行方式见 [测试说明](Tests/README.md)。
